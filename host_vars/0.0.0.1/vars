---
ansible_become: yes
ansible_become_password: "{{ vault_ansible_become_password }}"
ansible_user: "{{ vault_ansible_user }}"
ansible_ssh_private_key: "{{ vault_ansible_ssh_private_key }}"
nfs_server: "{{ vault_nfs_server }}"
nfs_export: "{{ vault_nfs_export }}"
